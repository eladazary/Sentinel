# Sentinel — Phase 0 stack: api · worker · TimescaleDB · Redis · frontend.
#
#   cp .env.example .env   # add Alpaca keys
#   docker compose up --build
#
# API:      http://localhost:8000
# Frontend: http://localhost:5173

services:
  timescaledb:
    image: timescale/timescaledb:2.17.2-pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: sentinel
      POSTGRES_PASSWORD: sentinel
      POSTGRES_DB: sentinel
    volumes:
      - timescale_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sentinel -d sentinel"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build: ./backend
    image: sentinel-backend:local
    command: ["api"]
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      # Override the .env defaults so containers reach each other by service name.
      SENTINEL_DATABASE_URL: postgresql+psycopg://sentinel:sentinel@timescaledb:5432/sentinel
      SENTINEL_REDIS_URL: redis://redis:6379/0
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - model_artifacts:/app/artifacts
    ports:
      - "8000:8000"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  worker:
    build: ./backend
    image: sentinel-backend:local
    command: ["worker"]
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    # Let the container reach an Ollama server running on the Mac host.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      SENTINEL_DATABASE_URL: postgresql+psycopg://sentinel:sentinel@timescaledb:5432/sentinel
      SENTINEL_REDIS_URL: redis://redis:6379/0
      # If SENTINEL_OLLAMA_MODEL is set in .env, reach host Ollama by default.
      SENTINEL_OLLAMA_BASE_URL: ${SENTINEL_OLLAMA_BASE_URL:-http://host.docker.internal:11434}
    depends_on:
      # Wait for the API so migrations have run before the worker touches tables.
      api:
        condition: service_healthy
    volumes:
      - model_artifacts:/app/artifacts
      - hf_cache:/root/.cache/huggingface  # persist the FinBERT download

  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "5173:80"

volumes:
  timescale_data:
  model_artifacts:
  hf_cache:
